export function Footer() {
  return (
    <footer className="mt-auto border-t border-hairline">
      <div className="mx-auto max-w-6xl px-4 py-6 text-xs text-faint sm:px-6">
        <p>Collect only — no likes, comments, or follows.</p>
      </div>
    </footer>
  );
}
