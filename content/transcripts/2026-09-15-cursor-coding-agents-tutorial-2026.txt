Developers are now writing a lot of code Developers are now writing a lot of code with agents. Instead of typing every with agents. Instead of typing every with agents. Instead of typing every line by hand, they now talk to an agent line by hand, they now talk to an agent line by hand, they now talk to an agent and have it write the code for them. In and have it write the code for them. In and have it write the code for them. In the last course, we learned the core the last course, we learned the core the last course, we learned the core behavior behind agents, giving them behavior behind agents, giving them behavior behind agents, giving them these powerful tools and letting them these powerful tools and letting them these powerful tools and letting them run autonomously in a loop. In this run autonomously in a loop. In this run autonomously in a loop. In this course, we're going to cover strategies course, we're going to cover strategies course, we're going to cover strategies to work effectively with coding agents to work effectively with coding agents to work effectively with coding agents to build software. Let's get started. Cursor is an AI editor with a built-in Cursor is an AI editor with a built-in coding agent. This agent runs inside coding agent. This agent runs inside coding agent. This agent runs inside what's called a harness, which is made what's called a harness, which is made what's called a harness, which is made up of three things. First are the up of three things. First are the up of three things. First are the instructions that you give to the model,