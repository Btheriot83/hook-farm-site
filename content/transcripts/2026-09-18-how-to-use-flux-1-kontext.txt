Flux Context is now available inside LTX Flux Context is now available inside LTX Studio, introducing a new paradigm in AI Studio, introducing a new paradigm in AI Studio, introducing a new paradigm in AI image generation. Built for creative image generation. Built for creative image generation. Built for creative control at every control at every control at every step, context is a multimodal model, step, context is a multimodal model, step, context is a multimodal model, which means it understands both images which means it understands both images which means it understands both images and text. This allows you to generate, and text. This allows you to generate, and text. This allows you to generate, edit, and transform visuals using edit, and transform visuals using edit, and transform visuals using natural language with contextaware natural language with contextaware natural language with contextaware precision. To get started, head to the precision. To get started, head to the precision. To get started, head to the image workspace from the LTX Studio home image workspace from the LTX Studio home image workspace from the LTX Studio home screen. This is where you'll access all screen. This is where you'll access all screen. This is where you'll access all the new capabilities inside Flux the new capabilities inside Flux the new capabilities inside Flux Context. Context has exceptional prompt Context. Context has exceptional prompt Context. Context has exceptional prompt adherence and it excels when you start